<h1>To Do List</h1>
  <p>
    <%= link_to 'New To Do Item', new_todo_path %>
  </p>

<% @todos.each do |todo| %>
  <h2><%= todo.name %> </h2>

  <%= link_to 'Show To Do List', todo_path(todo)%>
  <%= link_to 'Edit Item', edit_todo_path(todo)%>
  <%= link_to 'Desrty Item', todo, method: :delete%>
  <%end%>